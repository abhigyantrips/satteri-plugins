---
"satteri-abbr": minor
---

Add `satteri-abbr`, a plugin that wraps abbreviations in `<abbr>` elements with their definitions. Supports per-term aliases, a `firstOnly` mode, and a configurable `ignore` list of ancestor tags (default: `a`, `abbr`, `code`, `pre`).
